---
description: Handoff context for MSDS Manager KC — read before continuing work on Windows
alwaysApply: true
---

# MSDS Manager KC — session context

Before coding, read **AGENTS.md** at the repo root for full handoff instructions.

## Snapshot

- Windows .NET 8 WPF app + Outlook 2019 COM (not a web add-in)
- Phases 1–4 plus daily-use hardening (matching, Outlook errors, incremental index, undo last move)
- Built on Mac; **WPF Release build / Outlook tests only on Windows**
- Never send email automatically
- Never move files without explicit approval (Organise folders = suggest → approve → move; undo last batch)

## Folder organise decisions (locked)

- Suggest only
- Move (not copy) after approval
- Detect wrong folder as well as uncategorised
- Deep nesting supported (`Kitchen/Dishwashing`); parent category filter includes children

## Tomorrow’s default next step

1. `git pull` && `dotnet run --project src\MSDSManager\MSDSManager.csproj`
2. Point at real OneDrive SDS folder → re-index
3. Smoke-test library + Outlook + Organise folders with real files
4. Fix real-data / COM / move issues before adding new features

Do not invent a new architecture unless the user asks. Prefer small fixes against live SDS files and Outlook 2019.
